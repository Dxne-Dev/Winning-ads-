import type { Metadata } from "next";
import { Fira_Sans, Fira_Code } from "next/font/google";
import "./globals.css";

const firaSans = Fira_Sans({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
  variable: "--font-sans",
  display: "swap",
});

const firaCode = Fira_Code({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  variable: "--font-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Winning Ads AI — Meta Ads Intelligence",
  description:
    "AI-powered Meta Ads intelligence platform. Discover winning ads, understand why they work, and generate optimized versions.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`h-full antialiased ${firaSans.variable} ${firaCode.variable}`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
